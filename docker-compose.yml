services:
  app:
    build: .
    platform: linux/arm64
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      - HOSTNAME=0.0.0.0
      - PORT=3001
      # Optional — set these if you use Sentry
      # - SENTRY_DSN=
      # - NEXT_PUBLIC_SENTRY_DSN=
